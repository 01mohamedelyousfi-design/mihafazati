# scripts/.env.example
#
# Copy to scripts/.env.local and fill in real values. NEVER commit the .local file.
# The service-role key is a database password — treat it accordingly.

# Supabase project URL (Dashboard → Settings → API → Project URL)
SUPABASE_URL= https://svzqcfpuifvqvwubmkia.supabase.co/rest/v1/

# Supabase service-role key (Dashboard → Settings → API → service_role/secret).
# Has full DB + Storage bypass; never place in browser code.
SUPABASE_SERVICE_ROLE_KEY= sb_publishable_vLvgCOqyKpa5WjrPI9TXtg_rP5X9WgZ
